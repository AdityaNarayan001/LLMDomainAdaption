.venv*/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Large / generated artifacts — NOT committed.
# Leading slash = ROOT-ONLY, so these do NOT match src/data/, etc.
# (no inline comments — git would treat them as part of the pattern)
/data/
/models/
/runs/
*.parquet
*.bin
*.safetensors

# Build caches for the hyperswitch verification env.
target/
sccache/

# NOTE: eval_sets/ is intentionally COMMITTED (public repo) for reproducibility —
# EXCEPT generated artifacts (deterministically rebuilt by build_cpt / build_rl_synthetic):
/eval_sets/heldout_code.jsonl
/eval_sets/trivial/
